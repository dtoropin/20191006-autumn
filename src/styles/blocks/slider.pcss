.slider {
  position: relative;
}

.slider__preview {
  position: absolute;
  bottom: 50px;
  right: 0;
  z-index: 10;
}

.slider__control {
  position: absolute;
  bottom: 50px;
  left: 100%;
  z-index: 10;
}