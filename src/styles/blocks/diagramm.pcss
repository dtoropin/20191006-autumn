.diagramm {
  position: relative;
  transform: rotate(-90deg);
}

.diagramm__text {
  font-size: 18px;
  position: absolute;
  top: 50%;
  left: 50%;
  text-align: center;
  transform: translate(-50%, -50%) rotate(90deg);
  opacity: 0.7;
}